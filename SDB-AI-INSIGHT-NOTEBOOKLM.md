# SDB AI Insight — เอกสารอธิบายระบบ (ฉบับสำหรับ NotebookLM)

> เอกสารนี้เขียนเป็นความเรียงเพื่อให้ NotebookLM อ่านและสรุปได้ดี เหมาะสำหรับสร้าง Audio Overview, Briefing doc, Mind map และโครงสไลด์นำเสนอ เนื้อหาแบ่งเป็น 2 ส่วนหลักคือ (1) Flow การทำงานของระบบ และ (2) แต่ละหน้าในระบบทำอะไรและทำงานอย่างไร
>
> หมายเหตุเรื่องชื่อ: **SDB AI Insight** คือชื่อผลิตภัณฑ์และหน้า dashboard ที่ผู้ใช้เห็น ส่วนชื่อระบบเบื้องหลังในโค้ดเรียกว่า Claude Monitor ทั้งสองคือสิ่งเดียวกัน

---

# ส่วนที่ 1 — Flow การทำงานของระบบ SDB AI Insight

## SDB AI Insight คืออะไรและแก้ปัญหาอะไร

SDB AI Insight เป็นระบบติดตามการใช้งาน Claude AI ขององค์กร หน้าที่หลักคือดักจับทุกข้อความ (prompt) ที่พนักงานส่งไปหา Claude และทุกคำตอบ (response) ที่ได้กลับมา จากนั้นนำมาสรุปเป็นค่าใช้จ่ายเป็นเงินดอลลาร์สหรัฐ จำนวน token ที่ใช้ และพฤติกรรมการใช้งานของแต่ละคน แล้วแสดงผลทั้งหมดบนหน้า dashboard

ปัญหาที่ระบบนี้แก้คือ องค์กรจ่ายค่าบริการ Claude อยู่ทุกเดือนแต่มองไม่เห็นรายละเอียดว่าใครใช้บ้าง ใครใช้ไปเท่าไหร่ model ตัวไหนกินเงินมากที่สุด พนักงานใช้ผ่านช่องทางใด (เช่น แอป Claude Desktop, เว็บ Claude.ai, เครื่องมือ Cowork, Claude Code ทั้งแบบ command line และ VSCode extension หรือเรียกผ่าน API โดยตรง) และค่าใช้จ่ายพุ่งสูงผิดปกติในช่วงเวลาใด SDB AI Insight ตอบคำถามเหล่านี้ได้ทั้งหมดในที่เดียว

ระบบสร้างด้วยเทคโนโลยีหลักสี่ส่วน ได้แก่ mitmproxy ซึ่งเป็น proxy ที่เขียนด้วยภาษา Python ทำหน้าที่ดักจับ traffic, Cloudflare Workers ทำหน้าที่รับข้อมูลและแสดงผล, Cloudflare D1 ซึ่งเป็นฐานข้อมูล SQLite สำหรับเก็บ log และ Logto ซึ่งเป็นระบบยืนยันตัวตนมาตรฐาน OIDC สำหรับล็อกอินเข้า dashboard

## สถาปัตยกรรมภาพรวมสามชั้น

ระบบทำงานเป็นสามชั้นที่แยกหน้าที่กันชัดเจน

ชั้นแรกคือ Clients หมายถึงโปรแกรมต่างๆ ที่พนักงานใช้คุยกับ Claude ทุกตัว ทั้ง Claude Desktop, เว็บ Claude.ai, Cowork, Code tab ในแอป Desktop, Claude Code แบบ command line, VSCode extension และการเรียกผ่าน API SDK เครื่องของผู้ใช้แต่ละคนจะถูกตั้งค่าให้ส่ง traffic ทั้งหมดผ่าน proxy ที่ตำแหน่ง 127.0.0.1 พอร์ต 8080 ก่อนออกอินเทอร์เน็ต

ชั้นที่สองคือ Proxy ซึ่งคือ mitmproxy ที่โหลดสคริปต์ addon.py เข้าไป ชั้นนี้ทำหน้าที่ดักจับ traffic ด้วยเทคนิค TLS MITM แล้ววิเคราะห์ว่า traffic นั้นมาจาก client ตัวไหน ดึงข้อความที่ผู้ใช้พิมพ์จริงออกมา นับจำนวน token คำนวณราคา และตรวจหาอีเมลของเจ้าของ call นั้น จากนั้นจะทำสองอย่างพร้อมกันคือเขียนสำเนาลงไฟล์ในเครื่อง (รูปแบบ JSONL) เพื่อสำรองข้อมูล และส่งข้อมูลไปยัง Cloudflare Worker ผ่านการเรียก POST ที่ปลายทาง /log

ชั้นที่สามคือ Cloudflare Worker ทำงานร่วมกับฐานข้อมูล D1 ชั้นนี้รับ log ที่ส่งมา เติมข้อมูลตัวตนที่ขาดหายให้สมบูรณ์ เก็บลงฐานข้อมูล จัดการการล็อกอินผ่าน Logto และเรนเดอร์หน้า dashboard ทั้งหมดออกมาเป็น HTML ให้ผู้ใช้ดู

หลักการสำคัญคือ proxy รับหน้าที่เก็บข้อมูล ส่วน Worker รับหน้าที่จัดเก็บถาวรและแสดงผล สองหน้าที่นี้แยกกันชัดเจน ทำให้ดูแลและขยายระบบได้ง่าย

## ลำดับการเก็บข้อมูลหนึ่งครั้งแบบครบวงจร

เมื่อผู้ใช้พิมพ์ข้อความส่งไปหา Claude ระบบจะทำงานตามลำดับดังนี้

ขั้นแรก traffic ของผู้ใช้จะวิ่งผ่าน mitmproxy โดยอัตโนมัติ เพราะเครื่องถูกตั้งค่า system proxy ไว้แล้ว

ขั้นที่สอง สคริปต์ addon.py จะจับทั้ง request และ response แล้วประมวลผลหลายอย่าง ฟังก์ชัน detect_client จะระบุว่ามาจาก client ใด ฟังก์ชัน extract_prompt จะดึงเฉพาะข้อความที่ผู้ใช้พิมพ์จริงโดยข้ามส่วนที่ระบบแทรกเข้ามา ฟังก์ชัน parse_sse จะอ่านจำนวน token ทั้ง input output และ cache จาก stream ที่ตอบกลับ ฟังก์ชัน calc_cost จะคำนวณราคาเป็นดอลลาร์ และฟังก์ชัน current_email จะหาอีเมลของเจ้าของ call

ขั้นที่สาม ระบบจะตรวจผ่านฟังก์ชัน should_log ว่า call นี้ผ่านเงื่อนไขการกรองอีเมลหรือไม่ ถ้าผ่าน ระบบจะเขียนข้อมูลลงไฟล์ JSONL ในเครื่องเป็นการสำรอง และส่งข้อมูลไปยัง Worker ที่ปลายทาง /log แบบ fire-and-forget คือยิงแล้วไม่รอผล จึงไม่หน่วงการใช้งานของผู้ใช้

ขั้นที่สี่ Worker ที่ปลายทาง /log จะตรวจรหัส X-Api-Key ก่อน ถ้าอีเมลที่ส่งมาว่างเปล่า ระบบจะค้นหาจากตาราง ip_identity เพื่อเติมอีเมลให้จากที่อยู่ IP จากนั้นบันทึกข้อมูลลงตาราง api_logs และถ้า call นี้มีอีเมล ระบบจะอัปเดตตาราง ip_identity เพื่อจดจำว่า IP นี้เป็นของใคร ไว้ใช้เติมให้ call ครั้งต่อไปที่อาจไม่มีอีเมล

ขั้นสุดท้าย ข้อมูลจะปรากฏบน dashboard ทันที เพราะ dashboard ดึงข้อมูลสดจากฐานข้อมูล D1 ทุกครั้งที่เปิดหน้า

## ชั้น Proxy ทำงานอย่างไร

สคริปต์ addon.py ประกอบด้วยคลาสหลายตัว แบ่งเป็นกลุ่มที่เก็บข้อมูลจริงและกลุ่มที่ใช้ debug

กลุ่มที่เก็บข้อมูลจริงมีดังนี้ คลาส ClaudeAPIMonitor ดักการเรียกที่ api.anthropic.com/v1/messages ครอบคลุมทั้งผู้ที่ใช้ API key, Claude Code แบบ command line, VSCode, Cowork และ Code tab คลาส ClaudeDesktopMonitor ดักการเรียกที่ claude.ai ปลายทาง completion ซึ่งใช้โดยแอป Claude Desktop และเว็บ คลาส ClaudeBridgeMonitor ดักการเชื่อมต่อแบบ WebSocket ที่ bridge.claudeusercontent.com ซึ่งเป็นช่องทางของ Claude Code ที่ล็อกอินด้วยบัญชี subscription คลาส ClaudeAccountSniffer ทำงานก่อนเสมอเพื่ออ่านอีเมลของผู้ใช้จาก response ของ claude.ai และคลาส ToolSchemaFixer คอยแก้ schema ของเครื่องมือบางตัวที่ Anthropic API ปฏิเสธ เพื่อให้คำขอผ่านได้

กลุ่มที่ใช้ debug ได้แก่ ClaudeConnectionLogger ที่บันทึกชื่อโฮสต์ของทุกการเชื่อมต่อ TLS รวมถึง ClaudeDesktopDiscovery และ ClaudeBridgeDiscovery ที่บันทึก endpoint และข้อความที่ระบบยังไม่รู้จักไว้ศึกษาเพิ่ม

## การตรวจจับว่ามาจาก Client ใด

ระบบตรวจจับ client เป็นสองชั้นเรียงตามความน่าเชื่อถือ

ชั้นแรกตรวจจาก header ของ request โดยฟังก์ชัน detect_client จะอ่านค่า User-Agent, anthropic-client-name, x-app และ x-client-context ถ้าพบคำว่า claude-code ร่วมกับ electron จะระบุเป็น claude-desktop-code ถ้าพบ claude-code ร่วมกับ vscode จะเป็น claude-code-vscode ถ้าพบ claude-code เพียงอย่างเดียวจะเป็น claude-code-cli ถ้าพบ electron หรือ anthropic ใน User-Agent จะเป็น claude-desktop และถ้าไม่เข้าเงื่อนไขใดเลยจะเป็น api

ชั้นที่สองตรวจจากเนื้อหาของ request เมื่อ header ไม่ชัดเจน ถ้าเนื้อหามีเครื่องมือชื่อขึ้นต้นด้วย mcp__cowork จะระบุเป็น claude-desktop-cowork ซึ่งชนะเงื่อนไขอื่นเสมอ และถ้าเนื้อหามีเครื่องมือสไตล์ Code เช่น Bash, Read, Write, Edit แต่ header ตรวจได้แค่ api จะปรับเป็น claude-code-cli แทน

## การระบุตัวตนแบบสี่ชั้น

ปัญหาที่พบบน proxy ส่วนกลางคือ log บางส่วนมีช่อง account_email ว่างเปล่า ในบางช่วงสูงถึง 25 ถึง 64 เปอร์เซ็นต์ สาเหตุเพราะผู้ใช้แบบ subscription บางคนไม่ได้ส่งอีเมลมาใน traffic ตรงๆ ระบบจึงออกแบบให้เก็บข้อมูลตัวตนกระจายไว้สี่ชั้น โดยแต่ละชั้นทำงานเป็นอิสระและรอดได้แม้ proxy หรือ worker จะรีสตาร์ท

ชั้นที่หนึ่งคือ ตัวแปร _ACCOUNT_BY_IP เก็บในหน่วยความจำของ proxy ใช้เป็นเส้นทางอ่านที่เร็วที่สุดตอนสร้าง log โดย map ที่อยู่ IP ไปยังอีเมล ชั้นที่สองคือไฟล์ account_slots.json บนดิสก์ของ proxy เป็นสำเนาของชั้นแรก ทำให้ข้อมูลรอดเมื่อ proxy รีสตาร์ท ชั้นที่สามคือตาราง ip_identity ในฐานข้อมูล D1 เป็นแหล่งความจริงระยะยาวที่รอดทุกสถานการณ์ และชั้นที่สี่คือคอลัมน์ client_ip ในตาราง api_logs ซึ่งบันทึกที่อยู่ IP ไว้กับทุกแถวเพื่อการตรวจสอบย้อนหลังและใช้แสดงผลสำรอง

ระบบได้อีเมลมาจากสามแหล่ง ได้แก่ การ sniff response ของ claude.ai การถอดรหัส JWT จาก Bearer token และการอ่าน handshake ตอนเชื่อมต่อ WebSocket ของ bridge หากสุดท้ายแล้วยังหาอีเมลไม่ได้เลย ระบบจะแสดงตัวตนเป็นรูปแบบ ip ตามด้วยที่อยู่ IP เช่น ip:10.10.84.42 แทน เพื่อให้ยังตรวจสอบได้ว่ามาจากเครื่องใด

## การคำนวณค่าใช้จ่าย

ราคาคิดเป็นดอลลาร์สหรัฐต่อหนึ่งล้าน token แบ่งตามระดับของ model ดังตาราง

| ระดับ Model | Input | Output | Cache Read | Cache Write |
|---|---|---|---|---|
| Opus | 15 | 75 | 1.50 | 18.75 |
| Sonnet | 3 | 15 | 0.30 | 3.75 |
| Haiku | 0.80 | 4 | 0.08 | 1.00 |

สูตรคำนวณคือ นำจำนวน input token คูณราคา input บวกกับ output token คูณราคา output บวกกับ cache read token คูณราคา cache read บวกกับ cache write token คูณราคา cache write แล้วหารด้วยหนึ่งล้าน เหตุที่ต้องแยกนับ cache read และ cache write เพราะราคาต่างกันมาก โดย cache read ถูกกว่า input ปกติประมาณสิบเท่า การแยกนับจึงทำให้คิดเงินได้แม่นยำ

## ชั้น Worker และระบบยืนยันตัวตน

Cloudflare Worker มีปลายทางสาธารณะที่ไม่ต้องล็อกอินได้แก่ /log สำหรับรับ log จาก proxy โดยตรวจ X-Api-Key, /health สำหรับตรวจสุขภาพระบบ, /login สำหรับ redirect ไป Logto, หน้าแรกที่มีพารามิเตอร์ code สำหรับรับ callback จาก OAuth และ /logout สำหรับจบ session ส่วนหน้า dashboard ทั้งหมดเป็นเส้นทางที่ต้องผ่านการตรวจสอบผู้ใช้ก่อนเสมอ

ระบบยืนยันตัวตนใช้ Logto ด้วยมาตรฐาน OIDC ร่วมกับ PKCE ขั้นตอนคือเมื่อผู้ใช้เข้า /login ระบบจะส่งไปหน้า authorize ของ Logto เมื่อล็อกอินสำเร็จ Logto จะส่งกลับมาที่ callback พร้อมรหัส code ระบบจะนำ code ไปแลก token แล้วตรวจสอบความถูกต้องของ id_token ด้วยกุญแจสาธารณะ JWKS จากนั้นสร้าง session เก็บใน cookie ชื่อ sid อายุเจ็ดวัน แล้วพาเข้า dashboard

## ฐานข้อมูล Cloudflare D1

ฐานข้อมูลประกอบด้วยตารางหลักดังนี้ ตาราง api_logs เก็บ log ทุก call ประกอบด้วยเวลา ชื่อ client อีเมลบัญชี ที่อยู่ IP ชื่อ model ข้อความ prompt จำนวน token ทุกประเภท และค่าใช้จ่าย ตาราง ip_identity เก็บการ map ที่อยู่ IP ไปยังอีเมลซึ่งเป็นชั้นที่สามของระบบตัวตน ตาราง sessions เก็บ session ของผู้ใช้ที่ล็อกอิน ตาราง oauth_state เก็บค่า state และ PKCE verifier ระหว่างกระบวนการ OAuth และตาราง app_settings เก็บค่าตั้งค่าของระบบในรูปแบบ key กับ value เช่น ingest key และสวิตช์การแจ้งเตือน เวลาทั้งหมดในระบบคำนวณตามเขตเวลา Asia Bangkok ซึ่งเร็วกว่า UTC เจ็ดชั่วโมง และเก็บค่าเวลาเป็น millisecond epoch

## ผลิตภัณฑ์ Claude ที่ระบบครอบคลุม

ระบบรองรับการดักจับจากทุกช่องทางหลักของ Claude ดังตาราง

| แหล่งที่มา | ช่องทาง | ป้ายกำกับ client |
|---|---|---|
| Claude Code CLI แบบ API key | api.anthropic.com/v1/messages | claude-code-cli |
| Claude Code CLI แบบ OAuth | bridge.claudeusercontent.com แบบ WebSocket | claude-code-cli |
| Claude Code VSCode | api.anthropic.com/v1/messages | claude-code-vscode |
| Claude Desktop chat | claude.ai ปลายทาง completion | claude-desktop |
| Claude.ai web chat | เหมือน Desktop chat | claude-desktop |
| Cowork บน Desktop | v1/messages แบบ beta | claude-desktop-cowork |
| Code tab บน Desktop | v1/messages แบบ beta | claude-desktop-code |
| Claude API SDK | api.anthropic.com/v1/messages | api |

## ข้อจำกัดที่ทราบ

ระบบมีข้อจำกัดบางอย่างที่ควรรู้ แอปบนมือถืออยู่คนละเครื่องจึงต้องดักที่ระดับเครือข่ายผ่าน router การเชื่อมต่อแบบ HTTP/3 หรือ QUIC mitmproxy ดักได้แค่ส่วนที่เป็น TCP ปัจจุบันยังไม่กระทบ ผู้ใช้แบบ API key ไม่มีอีเมลใน traffic จึงระบุได้แค่ที่อยู่ IP ซึ่งเป็นข้อจำกัดพื้นฐาน กรณี NAT หรือใช้ IP ร่วมกันหลายคนจะแยกตัวตนไม่ออก และ call แรกของ IP ใหม่ต้องรอให้ระบบ sniff หรือถอด JWT ก่อนหนึ่งครั้งจึงจะได้อีเมล

---

# ส่วนที่ 2 — แต่ละหน้าใน SDB AI Insight คืออะไรและทำงานอย่างไร

หน้า dashboard ของ SDB AI Insight แสดงเมนูหลักสามรายการที่แถบด้านข้างคือ Dashboard, Analytics และ Accounts ส่วนหน้าอื่นได้แก่ Settings, Monitoring, Data Sources, Reports, Insights และ Identity ถูกซ่อนจากแถบเมนูแต่ยังเข้าถึงได้ผ่าน URL โดยตรง

## หน้า Dashboard

หน้า Dashboard อยู่ที่เส้นทางหลักคือ slash เป็นหน้าหลักที่สรุปการใช้งานทั้งหมดพร้อมตัวกรองครบถ้วน ด้านบนมีแถบตัวกรองให้เลือกช่วงเวลาแบบรายวัน รายเดือน หรือรายปี เลือกวันที่เริ่มต้นและสิ้นสุด กรองตาม model บัญชี และ client พร้อมปุ่มส่งออกเป็นไฟล์ CSV และปุ่มยืนยันการกรอง

ถัดลงมาเป็นการ์ดสรุปตัวเลขหกใบ แสดงจำนวน call จำนวน token ขาเข้าและขาออก จำนวน cache read และ cache write และค่าใช้จ่ายรวม ถัดมาเป็นการ์ดสรุปสามใบที่แยกตาม model แยกตามบัญชี และแยกตาม client โดยเรียงตามค่าใช้จ่าย

ส่วนล่างสุดเป็นตารางรายการ call ล่าสุดทั้งหมด แต่ละแถวแสดงเวลา client บัญชี model ตัวอย่างข้อความ prompt จำนวน token และค่าใช้จ่าย ผู้ใช้เลือกจำนวนแถวต่อหน้าได้ตั้งแต่สิบถึงทั้งหมด มีระบบแบ่งหน้า และเมื่อคลิกที่ข้อความ prompt จะเปิดหน้าต่างแสดงข้อความเต็ม

## หน้า Accounts

หน้า Accounts อยู่ที่เส้นทาง accounts แสดงรายชื่อบัญชีทั้งหมดที่เชื่อมต่อ Claude พร้อมสรุปการใช้งานของแต่ละคน มีตัวกรองช่วงเวลาให้เลือกเจ็ดวัน สามสิบวัน เก้าสิบวัน หรือทั้งหมด และมีการ์ดสรุปตัวเลขรวมได้แก่จำนวนบัญชีทั้งหมด บัญชีที่ยังใช้งานอยู่ ค่าใช้จ่ายรวม จำนวน call รวม และค่าเฉลี่ยต่อบัญชี

ตารางบัญชีแต่ละแถวแสดงรูป avatar ที่มีสีสร้างจากการ hash อีเมล อีเมล จำนวน call จำนวน token model ที่ใช้บ่อย ค่าใช้จ่าย และสถานะ โดยสถานะแบ่งเป็นสามแบบคือ live หมายถึงใช้งานภายในหนึ่งชั่วโมงที่ผ่านมา idle หมายถึงภายในเจ็ดวัน และ cold หมายถึงเกินเจ็ดวัน เมื่อคลิกที่แถวจะพาไปยังหน้ารายละเอียดของบัญชีนั้น

## หน้า Account Detail

หน้า Account Detail อยู่ที่เส้นทาง account พร้อมพารามิเตอร์ identity ซึ่งรับได้ทั้งอีเมลและรูปแบบ ip หน้านี้เจาะลึกการใช้งานของบัญชีเดียว มีตัวกรองช่วงเวลาตั้งแต่ยี่สิบสี่ชั่วโมงถึงทั้งหมด และกรองตาม client หรือ model ได้

เนื้อหาประกอบด้วยการ์ดสรุปตัวเลขของบัญชีนั้น ทั้งจำนวน call จำนวน token ค่าใช้จ่ายรวม เวลาที่เห็นครั้งแรกและครั้งล่าสุดพร้อมสถานะ มีกราฟค่าใช้จ่ายตามเวลาในรอบสามสิบวัน มีการแยกตาม model และ client ของคนนี้โดยเฉพาะ มีรายการ prompt ที่ใช้บ่อยห้าอันดับพร้อมจำนวนครั้งและค่าเฉลี่ย มีส่วนแสดงสัดส่วนการใช้ token มีแผนภาพความถี่การใช้งานแบบ heatmap ขนาดเจ็ดวันคูณยี่สิบสี่ชั่วโมง และมีรายการ prompt ล่าสุดห้าสิบรายการ

## หน้า Analytics

หน้า Analytics อยู่ที่เส้นทาง analytics ใช้ดูแนวโน้มค่าใช้จ่ายและการใช้ token เชิงลึกตามช่วงเวลา เลือกได้เจ็ดวัน สามสิบวัน หรือเก้าสิบวัน ด้านบนมีแถบสรุปตัวเลขรวมได้แก่ค่าใช้จ่ายรวม จำนวน call ค่าเฉลี่ยต่อ call จำนวน token ขาเข้าและขาออก และ cache read กับ cache write

หน้านี้มีกราฟห้าตัว ซึ่งอธิบายได้ดังนี้

กราฟแรกชื่อ Cost over time คือราคาที่จ่ายไปกับการใช้งานแยกตามช่วงเวลา ตัวอย่างเช่นในข้อมูลชุดหนึ่งมีสองวันคือวันที่สิบแปดเดือนพฤษภาคมและวันที่สิบเก้าเดือนพฤษภาคม โดยหนึ่งจุดหรือหนึ่งแท่งเท่ากับหนึ่งวัน

กราฟที่สองชื่อ Cost by model คือการเทียบแนวโน้มค่าใช้จ่ายของแต่ละ model สูงสุดเจ็ดตัว เพื่อดูว่า model ตัวไหนกินเงินมากที่สุด

กราฟที่สามชื่อ Token mix over time คือจำนวน token ที่ใช้แบ่งตามเวลา คำว่า mix หมายถึงการรวมสี่ส่วนซ้อนกันเป็นพื้นที่ ได้แก่ cache read, cache write, input และ output

กราฟที่สี่ชื่อ Calls timeline คือจำนวนครั้งที่เรียกใช้หรือ requests รวมกัน เรียงตามช่วงเวลาแยกตามวัน สิ่งสำคัญที่ต้องระวังคือกราฟนี้นับเป็นจำนวน call ต่อวัน ไม่ใช่ยอดเงิน ถ้าต้องการสื่อเรื่องค่าใช้จ่ายรวมต่อวันให้ใช้กราฟ Cost over time แทน

กราฟที่ห้าชื่อ Activity heatmap คือความถี่ของจำนวนครั้งการ prompt ในแต่ละช่วงเวลาของวันนั้นๆ แสดงเป็นตารางเจ็ดวันคูณยี่สิบสี่ชั่วโมง ช่องที่สีเข้มกว่าหมายถึงใช้งานบ่อยกว่า โดยอ้างอิงเวลา Bangkok

## หน้า Monitoring

หน้า Monitoring อยู่ที่เส้นทาง monitoring แสดงสถานะระบบแบบเรียลไทม์โดยดูย้อนหลังยี่สิบสี่ชั่วโมง มีการ์ดตัวเลขสำคัญได้แก่จำนวน call ในยี่สิบสี่ชั่วโมง อัตราความผิดพลาด จำนวน session ที่ยังใช้งานอยู่ในห้านาทีล่าสุด และสถานะระบบ ถัดมามีกราฟสุขภาพ API แสดงอัตราความผิดพลาดรายชั่วโมง รายการ session ที่กำลังใช้งานพร้อม client ตัวตน และ model ล่าสุด กราฟ throughput แสดงจำนวน call ต่อนาที ตารางกลุ่มความผิดพลาดสูงสุดจัดกลุ่มตาม client และ model และส่วนกฎการแจ้งเตือนแบบอ่านอย่างเดียว นิยามของความผิดพลาดในที่นี้คือ call ที่มี output token เป็นศูนย์และค่าใช้จ่ายเป็นศูนย์ ซึ่งหมายถึงไม่มีคำตอบกลับมา

## หน้า Data Sources

หน้า Data Sources อยู่ที่เส้นทาง data-sources ใช้จัดการแหล่งข้อมูลและ proxy ที่ป้อนข้อมูลเข้าระบบ ประกอบด้วยส่วนข้อมูล Claude API Proxy ส่วน ingest endpoint ที่แสดง URL ของ /log พร้อม ingest key ที่ถูกปิดบังบางส่วน ส่วนสถิติฐานข้อมูล D1 ที่แสดงจำนวนแถวรวม จำนวนแถววันนี้ ช่วงเวลาตั้งแต่เก่าสุดถึงใหม่สุด และขนาดข้อมูลโดยประมาณ และส่วนคำแนะนำการตั้งค่า proxy ให้ client

## หน้า Identity

หน้า Identity อยู่ที่เส้นทาง identity แสดงตารางการจับคู่ระหว่างที่อยู่ IP กับอีเมลในปัจจุบัน ซึ่งคือชั้นที่สามของระบบตัวตน ใช้เติมอีเมลให้ log ที่ระบุตัวตนไม่ได้ ตารางแสดงที่อยู่ IP อีเมล ชื่อ uuid เวลาอัปเดตล่าสุด และจำนวน call ที่ผูกกับ IP นั้น เรียงตามเวลาที่อัปเดตล่าสุด

## หน้า Reports

หน้า Reports อยู่ที่เส้นทาง reports ใช้สร้างรายงานและส่งออกข้อมูลเป็น CSV มีส่วนส่งออกแบบด่วนสำหรับวันนี้ เดือนนี้ ปีนี้ และทั้งหมด มีส่วนสร้างรายงานแบบกำหนดเองที่เลือกช่วงวันที่ กรอง model และเลือกคอลัมน์ได้ และมีส่วนประวัติการส่งออกซึ่งยังอยู่ระหว่างพัฒนา การส่งออกจริงทำงานผ่านเส้นทาง export พร้อมพารามิเตอร์วันที่เริ่มต้นและสิ้นสุด แล้วคืนไฟล์ CSV กลับมา

## หน้า Settings

หน้า Settings อยู่ที่เส้นทาง settings ใช้จัดการ API key และการแจ้งเตือน มีส่วนโปรไฟล์แสดงอีเมลผู้ใช้ปัจจุบัน ส่วน API key สำหรับ ingest ที่แสดงแบบปิดบังพร้อมปุ่มหมุนเปลี่ยน key โดยหลังเปลี่ยนแล้ว proxy เดิมจะใช้ต่อไม่ได้จนกว่าจะอัปเดต config ส่วนการแจ้งเตือนที่เก็บใน app_settings มีสามตัวเลือกคือสรุปรายวันทางอีเมล การแจ้งเตือนเมื่อค่าใช้จ่ายผิดปกติ และการแจ้งเตือนเมื่อใกล้ถึงงบประมาณ และส่วนข้อมูลเกี่ยวกับระบบที่แสดง version เขตเวลา ผู้ให้บริการตัวตนคือ Logto และ runtime คือ Cloudflare Workers กับ D1

## หน้า Clear Data

หน้า Clear Data อยู่ที่เส้นทาง clear-data ใช้ลบข้อมูลในระบบและต้องใช้อย่างระมัดระวัง ลบได้สี่แบบคือลบ log ทั้งหมด ลบตามช่วงวันที่ ลบตามตัวกรอง client บัญชี หรือ model และลบ session ทั้งหมดซึ่งจะทำให้ผู้ใช้ทุกคนถูก logout ทุกครั้งที่ลบระบบจะแสดงข้อความยืนยันจำนวนแถวที่ถูกลบ

## หน้า Insights

หน้า Insights อยู่ที่เส้นทาง insights เป็นส่วนที่ยังอยู่ระหว่างพัฒนา จะใช้แสดงข้อค้นพบที่ขับเคลื่อนด้วย AI จากการใช้งานจริง

---

# สรุปประเด็นสำคัญ

SDB AI Insight ดักจับการใช้งาน Claude ได้จากทุกช่องทางในที่เดียว ทั้ง Desktop เว็บ Cowork Code tab CLI VSCode และ API คิดค่าใช้จ่ายได้แม่นยำโดยแยกราคา input output และ cache ตามจริงของแต่ละ model ระบุตัวตนได้ทนทานด้วยกลไกสี่ชั้นที่รอดแม้ proxy หรือ worker รีสตาร์ท เก็บ log แบบไม่หน่วงผู้ใช้ด้วยวิธี fire-and-forget พร้อมสำรองเป็นไฟล์ในเครื่อง และมี dashboard ครบทั้งภาพรวม รายบุคคล แนวโน้ม การเฝ้าระวังแบบเรียลไทม์ และการส่งออกข้อมูล โดยทำงานบนสถาปัตยกรรม mitmproxy ส่งต่อไปยัง Cloudflare Workers และ D1 ยืนยันตัวตนด้วย Logto และแสดงผลเป็นหน้า dashboard
